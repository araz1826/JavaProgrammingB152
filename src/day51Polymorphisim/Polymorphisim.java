package day51Polymorphisim;

//Interface: Does not have a construct, instance field and abstract class
   //only interface has static final Constant and default method

public class Polymorphisim {
    /*
   Implements Comparabel<Person> or any class name
          CompareTo(), in this wAY Collection.sort(list), will be active otherwise will give error. you have to have a
                                       compareTo and implements comparable


POLYMORPHISM: it means multiple form of object, IS-A relationship.
        animal a1= new animal();
        animal a2= new horse(

Shape s1 = new Triangle();
      s1= new Circle();
      s1= new Rectangle();
      s1= new Square();
                         these are a relationship

               */

}
