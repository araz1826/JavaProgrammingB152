package day46;

public class MethodOVERRIDING {
    //how to call super classes constructor: by super(). but you can not inherit but still call it
    //default is super(), if you put super(int a) will call constructor with 1 argument
}
//OVERRIDING: when does it happen: only happen when there is parent child or super sub relationship.
            //only for inheritance