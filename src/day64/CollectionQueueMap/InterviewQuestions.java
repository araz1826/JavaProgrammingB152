package day64.CollectionQueueMap;

import java.util.SortedSet;

public class InterviewQuestions {
    public static void main(String[] args) {

         /* 1.Difference between HashMap & HashSet?
          Hashset: implemetation of set interface, store unique elemente per item, it can have dups
          HasMap: Implementation of map interface, store key value pair, keys are unique, no dups

          2. Difference between HashMap & LikedHasMap & TreeMap
          Hashmap is not puttin in order, faster, default implementation, no sorted order
          LinkedHasMap keep in order, based on your enters. (insertion order), so sorted order
          TreeMap: sorted map implementation, sort the item



*/

    }
}
