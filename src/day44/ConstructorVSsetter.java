package day44;

public class ConstructorVSsetter {
    //Constructor is good when you want to create an object with initial value(attributes )
    //Setter is good when you want to change the value

    //Interview: IMMUTABLE: make it private but still can be change so you need to add getter but no setter method
    //ex: FOR IMMUTABLE CONSTRUCTOR.
    // public class Person{
    /*string name;
    int age;
    public Person(String name, int age){
        this.name=name;
        this.age=age;

        Public String getName(){
            return name;
        }
        public int getAge(){
            return age;
        }*/
    //Person p1=new Person("araz", 27); // you can read but not able to change because immutable since you did not put setter

    //anything is not primitive called reference type
    }

